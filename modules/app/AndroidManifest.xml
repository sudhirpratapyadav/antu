<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.antu"
    android:versionCode="1"
    android:versionName="0.1">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="30" />

    <!-- Android 11 hides other packages unless they are declared, and without
         this ARCore reports itself as not installed. -->
    <queries>
        <package android:name="com.google.ar.core" />
    </queries>

    <!-- The graph outlives the activity: a robot must keep running with the
         screen off, so the work belongs in a foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Declared but not required, so the app installs on a device missing any
         of them and the affected driver simply reports itself unavailable. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <application android:label="antu"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Optional, so the app still installs and drives on a device without
             ARCore; the tracker node reports itself unavailable instead. -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />
        <!-- Contributed by ARCore's own manifest, which this build does not
             merge. Session creation throws without it. -->
        <meta-data android:name="com.google.ar.core.min_apk_version" android:value="232620000" />

        <activity android:name=".app.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The graph holds the camera, so the service must declare it or Android
             11 refuses access while the activity is not in the foreground. -->
        <service android:name=".app.RobotService" android:exported="false"
                 android:foregroundServiceType="camera" />
    </application>
</manifest>
